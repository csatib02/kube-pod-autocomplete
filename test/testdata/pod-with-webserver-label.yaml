apiVersion: v1
kind: Pod
metadata:
  name: pod-with-webserver-label
  labels:
    app.kubernetes.io/component: webserver
spec:
  containers:
  - name: pod-with-webserver-label
    image: nginx
    ports:
    - containerPort: 80
    resources:
      limits:
        cpu: "1"
        memory: "512Mi"
