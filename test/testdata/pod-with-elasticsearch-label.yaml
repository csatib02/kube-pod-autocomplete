apiVersion: v1
kind: Pod
metadata:
  name: pod-with-elasticsearch-label
  labels:
    app.kubernetes.io/name: elasticsearch
spec:
  containers:
  - name: pod-with-elasticsearch-label
    image: nginx
    ports:
    - containerPort: 80
    resources:
      limits:
        cpu: "1"
        memory: "512Mi"
