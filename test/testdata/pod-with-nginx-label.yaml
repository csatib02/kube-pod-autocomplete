apiVersion: v1
kind: Pod
metadata:
  name: pod-with-nginx-label
  labels:
    app.kubernetes.io/name: nginx
spec:
  containers:
  - name: pod-with-nginx-label
    image: nginx
    ports:
    - containerPort: 80
    resources:
      limits:
        cpu: "1"
        memory: "512Mi"
