apiVersion: v1
kind: Pod
metadata:
  name: pod-in-prod-ns
  namespace: prod
spec:
  containers:
  - name: pod-in-prod-ns
    image: nginx
    ports:
    - containerPort: 80
    resources:
      limits:
        cpu: "1"
        memory: "512Mi"
